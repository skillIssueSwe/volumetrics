#version 450

layout(local_size_x = 2, local_size_y = 8) in;

// Target image
layout(set = 0, binding = 0, rgba8) uniform writeonly image2D img;

// TODO Need to pass volume data

layout(push_constant) uniform PushConstants {
    vec3 cam_position;
    vec3 cam_angles;
} push_constants;

void main() {

}